<resources>
    <style name="Theme.GGVibe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Keep minimal; Compose handles most theming -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>